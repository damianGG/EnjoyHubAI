# Cloudinary Configuration
# Sign up at https://cloudinary.com and get your credentials from the Dashboard
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# OAuth Configuration
# For Facebook OAuth: https://developers.facebook.com/apps/
# For Instagram OAuth: Instagram uses Facebook Login (same app)
# Configure these in Supabase Dashboard > Authentication > Providers
# Facebook App ID and Secret should be configured in Supabase
# Redirect URL: {NEXT_PUBLIC_SITE_URL}/auth/callback

# Site URL for OAuth redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3000
